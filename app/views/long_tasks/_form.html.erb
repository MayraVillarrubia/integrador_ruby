<%= form_for(long_task) do |f| %>
  <% if long_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(long_task.errors.count, "error") %> prohibited this long_task from being saved:</h2>

      <ul>
      <% long_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :percentage_completion %>
    <%= f.number_field :percentage_completion %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
