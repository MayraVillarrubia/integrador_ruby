<%= form_for(temporary_task) do |f| %>
  <% if temporary_task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(temporary_task.errors.count, "error") %> prohibited this temporary_task from being saved:</h2>

      <ul>
      <% temporary_task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.datetime_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.datetime_select :end_date %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
